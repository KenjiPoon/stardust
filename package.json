{
  "name": "stardust-local",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "start": "node server.mjs --open", "stop": "node server.mjs --stop", "test": "node --test tests/*.test.mjs" },
  "engines": { "node": ">=18" }
}
