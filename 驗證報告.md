# 宇宙星塵：驗證報告

## GitHub Pages 正式網址驗證

2026-09-22：已部署至 **https://kenjipoon.github.io/stardust/**，GitHub Pages 建置成功並強制 HTTPS。已在 Chrome 從正式網址重新執行上述 7 項檢查，全部通過，沒有未捕捉 JavaScript 錯誤或失敗的資源請求。

- 正式網站中的 GPU Worker 已辨識官方真實手部照片；取樣 800 粒、60 FPS、辨識 18 毫秒。這次為功能驗證取樣，不是長時間效能測試。
- 11 個頁面／程式／模型資源請求全部位於本站專案路徑；沒有呼叫本機停止通道或其他外部服務。
- 首頁、主程式、Worker、使用中的 WASM 與模型皆回應 HTTP 200，SHA-256 與本機檔案一致；WASM MIME 為 `application/wasm`。
- 完整結果見 `deployment-verification.json`。應用程式驗證版本為 `faa6beb`；後續提交只補驗證記錄及分享預覽圖。
- 網頁版需要網絡下載資源，沒有建立離線快取；原本完整資料夾的離線使用方式不變。
- 此輪模型驗證使用真實手照片串流，並非真人連續操作。原有真人手勢測試限制仍然適用。

## 網頁版部署前檢查

2026-09-22：以一般靜態伺服器的 `/stardust/` 子路徑，在 Chrome 額外通過 7 項檢查：滑鼠聚合／單次散開、小窗開關、全螢幕、真實手照片模型辨識、追蹤中斷／恢復、停止後釋放資源，以及所有資源均從專案子路徑載入。

測試沒有設定 COOP／COEP 標頭，`crossOriginIsolated` 為 false，模型仍能使用 GPU Worker 完成辨識；800 粒星塵取樣約 60 FPS。一般靜態伺服器不會建立本機停止事件通道。

以下 29 項為原本本機版本的完整驗證記錄，與網頁版檢查分開列出。

驗證日期：2026-09-22。平台：本機 macOS、Google Chrome；獨立測試視窗，沒有關閉用戶其他工作。

## 結果

**29 項自動檢查通過，另已成功開啟實際攝影機。** 原始結果摘要附於 `verification.json`。

| 範圍 | 結果與方法 |
|---|---|
| 手勢邏輯：6 項 | 用模擬的 21 點手部座標，驗證捏合／張手只散開一次、短暫雜訊、手大小及平移變化、鏡像移動、離開與恢復、彎曲手指不誤觸，以及錯誤訊息分類 |
| 瀏覽器整合：16 項 | 實際 Chrome 操作滑鼠聚合／散開、小窗、全螢幕、390px 窄畫面；模擬鏡頭拒絕／佔用／缺失與模型載入失敗；驗證資源釋放、離線與無未捕捉 JavaScript 錯誤 |
| 生命週期：5 項 | 真實手圖位移經模型辨識後方向正確；取消啟動、離線停止通道、拒絕無效停止 token，以及停止啟動程序時關閉瀏覽器鏡頭與 Worker |
| 最終檢查：2 項 | 人為降低繪圖幀率後自動由 800 粒降到 400 粒；最終畫面與滑鼠散開再驗證 |
| 實際攝影機 | 已取得真實 video track，狀態 live；本機模型已就緒並採 GPU delegate。測試後已關閉鏡頭與測試頁面 |

## 手部辨識證據

真實照片測試使用 MediaPipe 官方 `right_hands.jpg`。測試頁面將其中一隻手繪製到 640×480 canvas，再以本機串流送入作品原本的 `video → ImageBitmap → Worker → MediaPipe → 手勢解讀 → 粒子` 流程。

- 模型實際輸出手部關鍵點，沒有以預先寫好的座標取代模型辨識。
- 移動照片位置後，粒子的目標位置向正確鏡像方向移動。
- 清空手部影像後進入自由漂浮；恢復照片後重新追蹤。
- 捏合防抖、張手及單次散開的完整序列，另以模擬座標作可重現測試。

**尚未驗證：用戶真人在鏡頭前連續完成「移動 → 捏合 → 張手」的效果，以及不同光線、遮擋與手部姿勢下的穩定性。** 成功開啟攝影機不等於已驗證所有真人手勢。

## 流暢度

在 1440×960 Chrome 測試視窗，以 800 粒星塵配合真實手部照片串流，暖機後連續量度 30 秒：

- 15 個每兩秒取樣的繪圖幀率均約 **60 FPS**。
- 同期單次辨識耗時 **15–29 毫秒**；初次載入／首次辨識較慢。
- 人為將更新速率壓低至約 20 FPS 後，已驗證會自動降至 **400 粒**。這項測試用來驗證降載機制，不表示人為限速後會回復 30 FPS。

以上是此輪環境的量度，不保證其他解析度、同時開啟的程式或硬件負載下有相同表現。

## 離線與資料流

瀏覽器攔截並阻擋所有非本機請求，重新載入頁面並啟動 Worker／模型：**外部請求為 0，手部辨識成功**。沒有依賴外部 CDN、雲端推論、遠端字體或遙測。

攝影機只要求 video，不要求 audio。關閉鏡頭後檢查 track 狀態為 ended、Worker 不再存在；停止本機服務後亦會透過本機事件通道關閉這些資源。實際攝影機測試沒有錄影或儲存影像。

## 檔案與啟動

- 所有自製 JavaScript 已通過語法檢查，JSON 可正常解析。
- MediaPipe 1.0.1 壓縮包符合 npm 官方 metadata 的 SHA-512 integrity。
- 模型及依賴的 SHA-256 記錄於 `assets-manifest.json`。
- 本機首頁回應 HTTP 200；伺服器只綁定 127.0.0.1。
- `.command` 啟動／停止檔具可執行權限；資料夾與壓縮包都包含模型和完整應用原始碼。
